{
  "name": "@lastro/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "echo \"no unit tests in foundation yet\"",
    "build": "mkdir -p dist && echo \"stub build for worker\" > dist/index.js",
    "dev": "echo 'worker dev stub'",
    "check": "bun run typecheck && bun run lint && bun run test && bun run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4"
  }
}
