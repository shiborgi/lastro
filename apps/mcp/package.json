{
  "name": "@lastro/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test",
    "build": "mkdir -p dist && echo \"stub build for mcp\" > dist/index.js",
    "dev": "bun src/index.ts",
    "check": "bun run typecheck && bun run lint && bun run test && bun run build"
  },
  "dependencies": {
    "@lastro/application": "workspace:*",
    "@lastro/auth": "workspace:*",
    "@lastro/config": "workspace:*",
    "@lastro/contracts": "workspace:*",
    "@lastro/db": "workspace:*",
    "@lastro/domain": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "hono": "^4.6.12",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@lastro/api": "workspace:*",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4"
  }
}
