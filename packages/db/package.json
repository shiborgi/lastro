{
  "name": "@lastro/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "echo \"no unit tests in foundation yet\"",
    "build": "mkdir -p dist && echo \"stub build for db\" > dist/index.js",
    "migrate": "bun src/migrate.ts",
    "check": "bun run typecheck && bun run lint && bun run test && bun run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4",
    "drizzle-kit": "^0.24.2"
  },
  "dependencies": {
    "@lastro/domain": "workspace:*",
    "drizzle-orm": "^0.33.0",
    "postgres": "^3.4.4"
  }
}
